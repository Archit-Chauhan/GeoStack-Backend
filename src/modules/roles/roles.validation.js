'use strict';

// GET /roles takes no body/query/params — no Zod schema needed. This file exists
// purely so the module follows the standard routes/controller/service/validation shape.
module.exports = {};
